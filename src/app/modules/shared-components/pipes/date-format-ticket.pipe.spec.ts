import { DateFormatTicketPipe } from './date-format-ticket.pipe';

describe('DateFormatTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
