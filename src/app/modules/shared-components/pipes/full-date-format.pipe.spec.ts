import { FullDateFormatPipe } from './full-date-format.pipe';

describe('FullDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FullDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
